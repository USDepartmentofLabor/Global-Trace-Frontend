declare namespace ThemeContext {
  export type Theme = {
    background: Background;
    colors: Colors;
  };

  export type Background = {
    default: string;
  };

  export type Colors = {
    default: string;
  };
}

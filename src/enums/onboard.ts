export enum OnboardMenuEnum {
  PROFILE = 'my_profile',
  QUESTIONNAIRES = 'self_assessment_questionnaire',
  PARTNERS = 'manage_business_partners',
}

export enum PartnerTypeEnum {
  BROKER = 'broker',
  PROCESSING_FACILITY = 'processing_facility',
  TRANSPORTER = 'transporter',
  TRANSFORMATION_PARTNER = 'transformation_partner',
}

export enum OarIdEnum {
  NOT_ME = 'not_me',
}

export enum OarIdStatusEnum {
  NEW_FACILITY = 'NEW_FACILITY',
  POTENTIAL_MATCH = 'POTENTIAL_MATCH',
  MATCHED = 'MATCHED',
  ERROR_MATCHING = 'ERROR_MATCHING',
}
